
For a reverse SSH tunnel, from the client's perspective, it's:

ssh -L <LOCAL CLIENT IP>:<LOCAL CLIENT LISTENING PORT>:<SSH SERVER IP>:<SSH SERVER ACTIVE PORT>

ssh -R <SSH SERVER IP>:<SSH SERVER LISTENING PORT>:<LOCAL NETWORK IP>:<LOCAL NETWORK ACTIVE SERVICE>



